void main(void)
{
   
	char ARCHIVO;

	FILE* fp; //FILE POINTER (PUNTERO DE ARCHIVO)
	printf("COLOQUE LA RUTA O NOMBRE DEL ARCHIVO: ");
	scanf_s("%s", ARCHIVO, 255);
	fopen_s( ARCHIVO); //r es de lectura (read) // el nombre almacenado se lo da a fp para indicar que es un archivo 

	if (fp == NULL)
	{
		printf("No abre");
	
	else
	
		printf("ya jala ");
		scanf(ARCHIVO);
		strcp(guarda ARCHIVO);
		printf(guarda);
		printf(tiene i, palabras);
		//obtener palabras 
		//imprimir lo obtenido
		//no se 
		//contar palabras totales
		//obtener frecuencia de palabras
		//Aqui termina
	}
}
NO LO COPIES NO COMPILA 
